let area = internal._get_area(internal._module_file())

let build = internal._import(internal._get_file("build.rain"))
let escape = internal._import(internal._get_file("escape.rain"))
let utils = internal._import(internal._get_file("utils.rain"))
