let std = import("std.rain")

let go = std.escape.bin("go")

// This is experimental
pub let build = fn(package: Dir, outname: String) -> File {
	result = std.utils.run(unit, go, "-o", outname, package)
	if !result.success {
		std.utils._throw(result)
	}
	std.fs.file(result.area, outname)
}
