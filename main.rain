let cargo = std.escape.bin("cargo")

fn check() {
	core.print("Checking...")
	let out = std.run(cargo, "clippy", "--workspace", "--", "--deny", "warnings")
	if out.success {
		core.print("Success")
	} else {
		core.error("Clippy Failed")
	}
	asdfl
	core.error("test")
	# std.run(cargo, "fmt", "--check")
	# std.run(cargo, "test", "--workspace")
	# std.run(cargo, "outdated", "--workspace")
}

check()
