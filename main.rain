fn foo() {
	std.print("world")
	return "hello"
}

std.print(foo())

let cargo = std.escape.bin("cargo")

# std.print(cargo)
# std.run(cargo, "clippy")
# std.run(cargo, "fmt", "--check")
# std.run(cargo, "test")
