let a = 5
let b = a
let foo = internal.import("foo.rain")
let bar = internal.import("bar.rain")

fn main() {
	internal.print("foo.apples:", foo.apples)
	internal.print("foo.bananas:", foo.bananas)
	internal.print("bar.apples:", bar.apples)
	internal.print("bar.bananas:", bar.bananas)
}
