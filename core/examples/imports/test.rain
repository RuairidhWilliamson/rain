fn import(path) {
	internal._import(internal._get_file(path))
}

let a = 5
let b = a
let foo = import("foo.rain")
let bar = import("bar.rain")

fn main() {
	internal._print("foo.apples:", foo.apples)
	internal._print("foo.bananas:", foo.bananas)
	internal._print("bar.apples:", bar.apples)
	internal._print("bar.bananas:", bar.bananas)
}
