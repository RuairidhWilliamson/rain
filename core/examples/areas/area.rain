let hello_world_example_area = internal._local_area("../helloworld")
let hello_world = internal._import(internal._get_file(hello_world_example_area, "hello.rain"))

let main = fn() {
	internal._print("Hello world number:", hello_world.my_number)
	unzipped_area = internal._extract_zip(internal._get_file("area.zip"))
	inner = internal._import(internal._get_file(unzipped_area, "inner.rain"))
	internal._print("Inner value:", inner.value)
	inner.value
}
