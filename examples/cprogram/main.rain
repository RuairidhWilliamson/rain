lazy cc = core.import(core.path("./ctools.rain"))

# Build foo
pub fn build() {
    let out = core.path("foo")
    return cc.build(core.file(core.path("main.c")), out)
}

pub fn call_twice() {
    let b1 = build()
    let b2 = build()
}
