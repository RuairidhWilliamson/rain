lazy cc = core.import(std.path("./ctools.rain"))

fn build() {
    let out = std.path("./foo")
    cc.build(std.path("./main.c"), out)
    # let output_file = build.output_file(out)
    # return output_file.as_executable()
    return out
}

fn run() {
    let out = build()
    std.run(out)
}
