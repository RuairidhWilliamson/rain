fn build(src, out) {
    let clang = std.escape.bin("clang")
    let b = std.run(clang, args = [src, "-o", out])
    core.print(b)
    if b.success {
    } else {
        core.error("build failed")
    }
    std.generated(b, out)
}
